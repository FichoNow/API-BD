/**
 * Respuesta del endpoint DELETE /user/requests/:id
 *
 * De momento devolvemos un mensaje simple confirmando
 * que la solicitud se ha cancelado correctamente.
 */
export interface DeleteLeaveRequestResponse {
    message: string;
}